import { MaskUsername2Pipe } from './mask-username2.pipe';

describe('MaskUsername2Pipe', () => {
  it('create an instance', () => {
    const pipe = new MaskUsername2Pipe();
    expect(pipe).toBeTruthy();
  });
});
