import { QuestionsFilterPipe } from './questions-filter.pipe';

describe('QuestionsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
