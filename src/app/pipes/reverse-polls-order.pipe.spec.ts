import { ReversePollsOrderPipe } from './reverse-polls-order.pipe';

describe('ReversePollsOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new ReversePollsOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
