import { ExpiredRecentlyPipe } from './expired-recently.pipe';

describe('ExpiredRecentlyPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpiredRecentlyPipe();
    expect(pipe).toBeTruthy();
  });
});
